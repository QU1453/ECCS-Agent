# ECCS Agent 后端依赖（Python 3.11+）
# 安装：pip install -r requirements.txt
fastapi>=0.115
uvicorn>=0.30
langgraph>=0.2.0
langchain-openai>=0.2.0
python-dotenv>=1.0

# 桌面版（可选，运行 main.py 时再装）：
# pywebview>=5.0
